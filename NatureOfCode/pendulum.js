class Pendulum {
  constructor(origin, r) {
    this.location = createVector(x,y);
    this.origin = createVector(0,0);
    this.r;
    this.angle;
    this.aVelocity;
    this.aAcceleration;
    this.damping;

    ori
  }
}
